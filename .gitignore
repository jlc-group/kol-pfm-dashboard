node_modules/
.env
# .env.local / .env.production / .env.bak ฯลฯ ก็มีรหัสผ่านได้เหมือนกัน — กันทุกแบบ ยกเว้นไฟล์ตัวอย่าง
.env.*
!.env.example
# ไฟล์สำรอง .env / db อาจมีรหัสผ่านและคีย์ติดไปด้วย ห้าม commit เด็ดขาด
*.bak
*.parity-backup
dist/
*.log

# ข้อมูล dev แบบไฟล์ (มี hash รหัสผ่าน) — ไม่ commit
server/data/
# ไฟล์ที่อัปโหลด (ใบเสนอราคา/ใบแจ้งหนี้)
server/uploads/
